name: Contentful rebuild

on:
  repository_dispatch:
    types: [contentful-publish]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: contentful-rebuild
  cancel-in-progress: false

jobs:
  trigger-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: true

      - name: Configure git
        run: |
          git config user.name "contentful-bot"
          git config user.email "contentful-bot@users.noreply.github.com"

      - name: Empty commit to trigger Vercel
        run: |
          git commit --allow-empty -m "chore: rebuild from contentful publish [$(date -u +%Y-%m-%dT%H:%M:%SZ)]"
          git push origin HEAD:main
